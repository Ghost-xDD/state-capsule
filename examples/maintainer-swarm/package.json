{
  "name": "@state-capsule/maintainer-swarm",
  "version": "0.1.0",
  "description": "MaintainerSwarm — 4-agent demo for State Capsule",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node dist/main.js",
    "seed": "node dist/scripts/seed-task.js",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.40.0",
    "@state-capsule/sdk": "workspace:*",
    "dotenv": "^16.0.0",
    "openai": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
